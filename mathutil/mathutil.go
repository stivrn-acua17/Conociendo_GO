package mathutil

var Edad = 6 // Mayuscula para exportar

func Add(x, y int) int { // Mayuscula para exportar
	return x + y
}

func resta(x, y int) int { // miniscula se quedan como funciones privadas
	return x - y
}